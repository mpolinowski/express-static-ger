<section class="row grid">

    <!-- Take Images from gallery-quickinstallation.json for Gallery -->

    <% if (qiPic.length > 0) { %>
        <% for (i=0; i<qiPic.length; i++) { %>
            <div class="col-6 col-lg-4 mb">
                <a href="/Quick_Installation/<%= qiUrl[i] %>"><img src="<%= qiPic[i] %>" alt="<%= qiPicAlt[i] %>" class="img-fluid"></a>
            </div>
        <% } %>
    <% } %>

    <div class="row mb mt center-block">

        <div class="col-lg-8 mt">
            <p class="lead">INSTAR bietet einen Installations-Wizard, der Sie durch die ersten Schritte Ihrer Kamera-Einrichtung führt.</p>
        </div>

        <div class="col-lg-3 offset-lg-1 mt">
            <p>
                <a href="http://install.instar.de" target="_blank">
                    <button type="button" class="btn btn-primary btn-lg">Installations-Wizard</button>
                </a>
            </p>
        </div>

    </div>

</section>
